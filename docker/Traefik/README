# Traefik

Reverse proxy, sitting in front of the various services so they don't all need their own exposed port remembered separately. Set up going smoothly, but paused partway through to get a proper DNS server (Bind9) running first, so routing by hostname would actually make sense before finishing the rest of the Traefik config.