# Portainer

Docker's installed and managed through Portainer, running at `https://192.168.56.101:9443` (Host-Only network, same as Samba).

Started with a couple of hello-world containers just to confirm everything was working, then added Uptime Kuma for monitoring (see `docker/uptime-kuma/`).
