Icons come from dashboardicons.com. A couple of them needed more specific names than expected, `samba-server.png` not `samba.png`, `ubuntu-linux.png` not `ubuntu.png`. Worth checking the site directly for the exact filename rather than guessing.
